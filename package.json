{
  "name": "cueless",
  "version": "0.1.0",
  "private": true,
  "description": "IM-first control layer for dispatching agent runtimes",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "start": "pnpm --filter @cueless/api start",
    "test": "turbo run test",
    "test:e2e": "pnpm --filter @cueless/api test:e2e",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.8",
    "typescript": "^5.7.3"
  }
}